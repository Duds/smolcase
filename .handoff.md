## Handoff — SMOLCASE — 2026-08-31

**Last session:** Tested Voice Phase 1 on-device. Popping is gone. Added SpeechRecognizer sound suppression (notification/system volume zeroed during listening, restored on mute/stop) and ambient noise resilience (2s silence threshold). Recognizer now has human-readable error logging and mute toggle logging.

**Key observations from test:**
- Latency 3.5-10.7s (Gemma maxNumTokens=1024 is bottleneck — fix is 20260830-002)
- "directives" word repetition in LLM replies persists (20260830-001)
- Dials parsed correctly but LLM ignores them (20260830-004)
- Memory not recalled across turns (20260830-003)
- Multi-turn context not maintained (20260830-005)
- IntentRouter "clear" returns wrong text (shows tasks instead of confirming deletion)
- Pixel 8 is now Android 17 (SDK 37)

**Failing tooling to investigate:**
- `write to memory` / `MemoryStore` — memory facts set but not injected into LLM context effectively
- `write to tasks` / `IntentRouter` — reminder storage works but retrieval/display is buggy
- `write to constitution and soul` / Persona prompt — system prompt causes word repetition; dial framing too weak to affect output

**Start here:** Deep research into best practices for on-device LLM robot conversation systems. Focus areas: latency reduction strategies for Gemma 4 E2B (128 tokens, KV cache tuning), prompt engineering for deadpan personas without word repetition, memory injection patterns for small context windows, and dial effectiveness through output steering rather than prompt framing. Run analysis on the existing JSONL log at `/data/data/com.smolcase.companion/files/logs/conversations.jsonl` to baseline improvements.